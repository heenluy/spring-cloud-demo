spring:
    application:
        name: config-client-app
    cloud:
        config:
            discovery:
                enabled: true
                serviceId: configuration-server
        
eureka:
    client:
        server-url:
            defaultZone: http://localhost:8761/eureka        
